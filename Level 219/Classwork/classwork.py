# -- შექმენი table სახელად useraccount რომელსშიც შეძლებთ შემდეგი ინფორმაციების შეყვანას id, name, surname, age, email.

# CREATE TABLE useraccount (
#     id INTEGER UNIQUE,
#     name VARCHAR(100),
#     surname VARCHAR(100)
#     age INTEGER
#     email VARCHAR(100) UNIQUE
# );